# Diisi otomatis oleh `vercel env pull .env.local` setelah Blob & Redis dihubungkan.
BLOB_READ_WRITE_TOKEN=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Opsional — batas ukuran file dalam MB (default 100)
MAX_UPLOAD_MB=100
